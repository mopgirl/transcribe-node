import * as Express from 'express';
import notImplemented from '../src/controllers/S3AccessController';


const router = Express.Router();

/* GET users listing. */
router.get('/upload', notImplemented);


export default router;
